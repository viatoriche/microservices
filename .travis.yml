language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - "pip install ."
  - "pip install -r test-requirements.txt"
# command to run tests
script: nosetests microservices